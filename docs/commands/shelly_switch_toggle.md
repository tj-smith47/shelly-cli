## shelly switch toggle

Toggle switch state

### Synopsis

Toggle the state of a switch component on the specified device.

```
shelly switch toggle <device> [flags]
```

### Examples

```
  # Toggle switch on bedroom device
  shelly switch toggle bedroom

  # Toggle specific switch ID
  shelly switch flip bedroom --id 1
```

### Options

```
  -h, --help     help for toggle
  -i, --id int   Switch component ID (default 0)
```

### Options inherited from parent commands

```
      --config string     Config file (default $HOME/.config/shelly/config.yaml)
      --no-color          Disable colored output
  -o, --output string     Output format (table, json, yaml, template) (default "table")
  -q, --quiet             Suppress non-essential output
      --template string   Go template string for output (use with -o template)
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [shelly switch](shelly_switch.md)	 - Control switch components

###### Auto generated by spf13/cobra on 15-Dec-2025
