## shelly schedule delete-all

Delete all schedules

### Synopsis

Delete all schedules from a Gen2+ Shelly device.

```
shelly schedule delete-all <device> [flags]
```

### Examples

```
  # Delete all schedules
  shelly schedule delete-all living-room

  # Delete without confirmation
  shelly schedule delete-all living-room --yes
```

### Options

```
  -h, --help   help for delete-all
  -y, --yes    Skip confirmation prompt
```

### Options inherited from parent commands

```
      --config string     Config file (default $HOME/.config/shelly/config.yaml)
      --no-color          Disable colored output
  -o, --output string     Output format (table, json, yaml, template) (default "table")
  -q, --quiet             Suppress non-essential output
      --template string   Go template string for output (use with -o template)
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [shelly schedule](shelly_schedule.md)	 - Manage device schedules

###### Auto generated by spf13/cobra on 15-Dec-2025
