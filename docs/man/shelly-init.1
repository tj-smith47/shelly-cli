.nh
.TH "SHELLY" "1" "Feb 2026" "Shelly CLI" "User Commands"

.SH NAME
shelly-init - Initialize shelly CLI for first-time use


.SH SYNOPSIS
\fBshelly init [flags]\fP


.SH DESCRIPTION
Initialize the shelly CLI with a guided setup wizard.

.PP
This command walks you through:
  1. Configuration (theme, output format, aliases)
  2. Device discovery (find Shelly devices on your network)
  3. Device registration (give discovered devices friendly names)
  4. Shell completions (tab completion for commands)
  5. Cloud access (optional remote control via Shelly Cloud)
  6. Telemetry (optional anonymous usage statistics)

.PP
INTERACTIVE MODE (default):
  Run without flags for the full guided wizard.

.PP
DEFAULTS MODE (--defaults):
  Runs the full wizard with sensible defaults, no prompts.
  Discovers devices, registers with default names, installs completions.
  Combine with flags to override individual defaults (e.g., --defaults --theme nord).

.PP
INDIVIDUAL FLAGS:
  Pass specific flags to set values; other steps remain interactive.

.PP
Use --check to verify your current setup without making changes.


.SH OPTIONS
\fB--aliases\fP[=false]
	Install default command aliases (opt-in)

.PP
\fB--all-networks\fP[=false]
	Scan all detected subnets without prompting

.PP
\fB--api-mode\fP=""
	API mode: local,cloud,auto (default: local)

.PP
\fB--check\fP[=false]
	Verify current setup without making changes

.PP
\fB--cloud-email\fP=""
	Shelly Cloud email (enables cloud setup)

.PP
\fB--cloud-password\fP=""
	Shelly Cloud password (enables cloud setup)

.PP
\fB--completions\fP=""
	Install completions for shells: bash,zsh,fish,powershell (comma-separated)

.PP
\fB--defaults\fP[=false]
	Use sensible defaults for all prompts (no interactive questions)

.PP
\fB--device\fP=[]
	Device spec: name=ip:user:pass
\[la]repeatable\[ra]

.PP
\fB--devices-json\fP=[]
	JSON device(s): file path, array, or single object (repeatable)

.PP
\fB--discover\fP[=false]
	Run device discovery without prompting

.PP
\fB--discover-modes\fP="http"
	Discovery modes: http,mdns,coiot,ble,all (comma-separated)

.PP
\fB--discover-timeout\fP=2m0s
	Discovery timeout

.PP
\fB--force\fP[=false]
	Overwrite existing configuration

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for init

.PP
\fB--network\fP=[]
	Subnet(s) for HTTP probe discovery (repeatable)

.PP
\fB--no-color\fP[=false]
	Disable colors in output

.PP
\fB--output-format\fP=""
	Set output format: table,json,yaml (default: table)

.PP
\fB--telemetry\fP[=false]
	Enable anonymous usage telemetry (opt-in)

.PP
\fB--theme\fP=""
	Set theme (default: dracula)


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--config\fP=""
	Config file (default $HOME/.config/shelly/config.yaml)

.PP
\fB-F\fP, \fB--fields\fP[=false]
	Print available field names for use with --jq and --template

.PP
\fB-Q\fP, \fB--jq\fP=[]
	Apply jq expression to filter output (repeatable, joined with |)

.PP
\fB--log-categories\fP=""
	Filter logs by category (comma-separated: network,api,device,config,auth,plugin)

.PP
\fB--log-json\fP[=false]
	Output logs in JSON format

.PP
\fB--no-headers\fP[=false]
	Hide table headers in output

.PP
\fB--offline\fP[=false]
	Only read from cache, error on cache miss

.PP
\fB-o\fP, \fB--output\fP="table"
	Output format (table, json, yaml, template)

.PP
\fB--plain\fP[=false]
	Disable borders and colors (machine-readable output)

.PP
\fB-q\fP, \fB--quiet\fP[=false]
	Suppress non-essential output

.PP
\fB--refresh\fP[=false]
	Bypass cache and fetch fresh data from device

.PP
\fB--template\fP=""
	Go template string for output (use with -o template)

.PP
\fB-v\fP, \fB--verbose\fP[=0]
	Increase verbosity (-v=info, -vv=debug, -vvv=trace)


.SH EXAMPLE
.EX
  # Interactive setup wizard (recommended for first use)
  shelly init

  # Quick setup with sensible defaults (no prompts)
  shelly init --defaults

  # Defaults with a specific theme
  shelly init --defaults --theme nord

  # Defaults with force overwrite of existing config
  shelly init --defaults --force

  # Check current setup without changes
  shelly init --check

  # Register devices directly (other steps remain interactive)
  shelly init --device kitchen=192.168.1.100 --device bedroom=192.168.1.101

  # Run discovery without prompting (other steps remain interactive)
  shelly init --discover

  # Scan specific subnets
  shelly init --discover --network 192.168.1.0/24 --network 10.0.0.0/24

  # Full CI/CD setup (no prompts)
  shelly init --defaults \\
    --device kitchen=192.168.1.100 \\
    --theme dracula \\
    --no-color

  # With cloud credentials
  shelly init --defaults --cloud-email user@example.com --cloud-password secret
.EE


.SH SEE ALSO
\fBshelly(1)\fP
