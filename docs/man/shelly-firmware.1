.nh
.TH "SHELLY" "1" "Dec 2025" "Shelly CLI" "User Commands"

.SH NAME
shelly-firmware - Manage device firmware


.SH SYNOPSIS
\fBshelly firmware [flags]\fP


.SH DESCRIPTION
Manage firmware for Shelly devices.

.PP
Check for updates, update to the latest version, or rollback to a previous version.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for firmware


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--config\fP=""
	Config file (default $HOME/.config/shelly/config.yaml)

.PP
\fB--log-categories\fP=""
	Filter logs by category (comma-separated: network,api,device,config,auth,plugin)

.PP
\fB--log-json\fP[=false]
	Output logs in JSON format

.PP
\fB--no-color\fP[=false]
	Disable colored output

.PP
\fB-o\fP, \fB--output\fP="table"
	Output format (table, json, yaml, template)

.PP
\fB-q\fP, \fB--quiet\fP[=false]
	Suppress non-essential output

.PP
\fB--template\fP=""
	Go template string for output (use with -o template)

.PP
\fB-v\fP, \fB--verbose\fP[=0]
	Increase verbosity (-v=info, -vv=debug, -vvv=trace)


.SH EXAMPLE
.EX
  # Check for updates on a device
  shelly firmware check living-room

  # Check for updates on all registered devices
  shelly firmware check --all

  # Show firmware status
  shelly firmware status living-room

  # Update device firmware
  shelly firmware update living-room

  # Update to beta firmware
  shelly firmware update living-room --beta

  # Rollback to previous firmware
  shelly firmware rollback living-room

  # Download firmware file
  shelly firmware download ShellyPlus1PM 1.0.0 --output firmware.zip
.EE


.SH SEE ALSO
\fBshelly(1)\fP, \fBshelly-firmware-check(1)\fP, \fBshelly-firmware-download(1)\fP, \fBshelly-firmware-rollback(1)\fP, \fBshelly-firmware-status(1)\fP, \fBshelly-firmware-update(1)\fP
