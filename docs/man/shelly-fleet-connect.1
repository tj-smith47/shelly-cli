.nh
.TH "SHELLY" "1" "Dec 2025" "Shelly CLI" "User Commands"

.SH NAME
shelly-fleet-connect - Connect to Shelly Cloud hosts


.SH SYNOPSIS
\fBshelly fleet connect [flags]\fP


.SH DESCRIPTION
Connect to Shelly Cloud hosts for fleet management.

.PP
Requires integrator credentials configured via environment variables or config:
  SHELLY_INTEGRATOR_TAG - Your integrator tag
  SHELLY_INTEGRATOR_TOKEN - Your integrator token

.PP
By default, connects to all regions (EU and US). Use --host to connect
to a specific cloud host.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for connect

.PP
\fB--host\fP=""
	Specific cloud host to connect to

.PP
\fB--region\fP=""
	Region to connect to (eu, us)


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--config\fP=""
	Config file (default $HOME/.config/shelly/config.yaml)

.PP
\fB--log-categories\fP=""
	Filter logs by category (comma-separated: network,api,device,config,auth,plugin)

.PP
\fB--log-json\fP[=false]
	Output logs in JSON format

.PP
\fB--no-color\fP[=false]
	Disable colored output

.PP
\fB-o\fP, \fB--output\fP="table"
	Output format (table, json, yaml, template)

.PP
\fB-q\fP, \fB--quiet\fP[=false]
	Suppress non-essential output

.PP
\fB--template\fP=""
	Go template string for output (use with -o template)

.PP
\fB-v\fP, \fB--verbose\fP[=0]
	Increase verbosity (-v=info, -vv=debug, -vvv=trace)


.SH EXAMPLE
.EX
  # Connect to all regions
  shelly fleet connect

  # Connect to specific host
  shelly fleet connect --host shelly-13-eu.shelly.cloud

  # Connect to specific region
  shelly fleet connect --region eu
.EE


.SH SEE ALSO
\fBshelly-fleet(1)\fP
